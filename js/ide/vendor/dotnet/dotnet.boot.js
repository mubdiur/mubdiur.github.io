// Minimal boot config for the vendored .NET wasm runtime — the loader
// merges this with the config passed to dotnet.create() (which carries
// the framework assembly list). Kept empty on purpose.
export const config = {};
